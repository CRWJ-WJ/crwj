# 选题说明
## 1. 拟定题目
基于计算机视觉与机器学习的金属零部件表面缺陷智能检测与分级系统
## 2. 完成目标
1. 搭建B/S架构工业缺陷检测原型系统，支持图片上传、零部件表面缺陷检测、缺陷分级并生成检测报告。
2. 基于OpenCV完成图像预处理、降噪、ROI感兴趣区域提取，定位零件缺陷区域。
3. 搭建CNN深度学习模型，实现划痕、凹坑、毛刺等典型金属零部件缺陷分类识别与等级判定。
4. 使用遗传优化算法自适应调整视觉检测阈值，适配不同批次工件表面条件，完成参数闭环优化。
5. 后端保存检测记录、图片路径、算法参数，前端可视化展示检测结果；完成代码版本管理，提交文档与项目代码到GitHub仓库。
## 3. 涉及的技术方向
1. **计算机视觉技术**：图像灰度化、滤波降噪、边缘检测、ROI区域提取，使用OpenCV‑Python完成零部件图像预处理（上学期计算机视觉课程专题）。
2. **机器学习与深度学习技术**：构建CNN卷积神经网络，完成缺陷特征提取、缺陷分类与等级判定，基于PyTorch完成模型训练与推理（上学期机器学习/深度学习课程专题）。
3. **智能优化算法（运筹优化）**：遗传算法实现检测阈值自适应寻优，利用历史检测数据迭代更新视觉检测参数，实现系统自适应调参（上学期智能优化算法课程专题）。
4. 附加技术：FastAPI后端Web开发、数据库数据持久化、Git版本控制。# 方案设计
## 1. 功能需求分析
### 1.1 用户端功能
1. 文件上传：前端上传金属零部件图片，也可读取模拟产线相机图像输入。
2. 检测结果展示：展示原图、缺陷标注区域、缺陷类别、缺陷等级。
3. 检测报告：查看历史检测记录，生成简单检测分级报告。
4. 参数配置：可查看、修改部分视觉检测算法参数。
### 1.2 系统后台功能
1. 接收前端请求，调度视觉处理、CNN缺陷识别、遗传算法优化模块。
2. 将图像路径、检测时间、缺陷类型、缺陷等级、优化后的算法参数存入数据库。
3. 提供历史数据供遗传优化算法读取，完成参数闭环更新。
### 1.3 非功能需求
1. B/S架构，浏览器即可访问系统；
2. 检测结果可溯源，所有检测记录持久保存；
3. 使用Git进行版本管理，小步提交，留存完整开发过程。
## 2. 方案论证
- 方案一：仅本地Python脚本实现检测。只可在本地运行，无Web界面，无法做参数配置与历史记录管理，不满足课程设计B/S系统要求，舍弃。
- 方案二：B/S架构，FastAPI后端 + 前端网页 + OpenCV视觉处理 + CNN深度学习模型 + 遗传优化算法 + 数据库存储。
前端负责人机交互，后端作为调度中心，依次调用图像预处理、缺陷识别、参数优化模块；数据库保存全部检测数据，为优化算法提供历史样本。技术均为课程所学，使用NEU公开工业缺陷数据集，适合课程设计实现，选用本方案。
## 3. 方案规划
### 3.1 技术栈
- 前端：HTML5、CSS3、JavaScript，实现图片上传、结果可视化展示      根据以上内容给我一个数据集的链接进行数据的预处理写代码
- 后端：Python + FastAPI，提供接口，调度各个算法模块
- 计算机视觉库：OpenCV‑Python
- 深度学习框架：PyTorch，CNN缺陷分类模型
- 优化算法：自定义遗传算法
- 数据库：SQLite（轻量化，课程设计无需额外安装服务）
- 版本控制：Git + GitHub
### 3.2 需配置的外部资源
1. Python环境，安装OpenCV、PyTorch、FastAPI等依赖库；
2. GitHub仓库用于存放代码与两份md文档；
3. 本地文件夹存储上传的零部件图片；
4. NEU表面缺陷数据集、飞桨工业质检数据集。
### 3.3 数据集来源
1. 主要数据集：NEU表面缺陷数据库（东北大学公开热轧带钢缺陷数据集，1800余张灰度图像，6类典型缺陷）。
2. 补充数据集：飞桨AI Studio工业质检公开数据集。
3. 数据增强手段：图像翻转、旋转、加噪声，扩充样本数量，提升模型泛化能力。
### 3.4 前端UI方案
页面划分：首页图片上传页面、检测结果展示页面、历史检测记录页面、算法参数查看配置页面。
前端实现：图片上传组件；接收后端返回的缺陷标注信息，在页面绘制缺陷区域；展示缺陷类别、缺陷等级；查询历史检测记录；简单表单修改算法部分参数。
### 3.5 后端算法方案
1. OpenCV视觉模块：完成图像灰度化、滤波降噪、边缘检测，提取缺陷ROI感兴趣区域。
2. CNN推理模块：对ROI区域进行特征提取，输出缺陷类别（划痕、凹坑、毛刺等）与缺陷等级。
3. 遗传算法优化模块：读取数据库历史检测数据，迭代优化亮度、相似度等视觉检测阈值，将优化后参数反馈给OpenCV预处理模块，实现自适应调参。
4. 数据库模块：存储原始图像路径、检测时间、缺陷类别、缺陷等级、优化后的算法参数，支持溯源与优化算法读取历史样本。
## 4. 技术路线
1. 初始化GitHub仓库，提交选题说明.md文档。
2. 数据集下载、预处理与数据增强，完成数据集划分训练集、测试集。
3. 训练CNN缺陷分类模型，保存模型权重文件。
4. 编写OpenCV图像预处理与ROI提取代码；实现遗传算法参数优化模块。
5. FastAPI搭建后端接口，串联图像预处理、缺陷识别、优化算法、数据库存储。
6. 开发前端页面，对接后端接口，完成图片上传、结果展示、历史记录查询。
7. 整体系统联调，功能测试，修复BUG。
8. 提交方案设计.md，推送全部项目代码至GitHub仓库。
## 5. 计划安排
1. 阶段一：确定课题，调研数据集，完成选题说明.md，提交GitHub仓库。
2. 阶段二：完成方案设计文档，数据集处理，CNN模型训练。
3. 阶段三：开发OpenCV视觉模块、遗传优化算法模块。
4. 阶段四：FastAPI后端接口开发，数据库设计。
5. 阶段五：前端页面开发，前后端联调。
6. 阶段六：系统整体测试，完善文档，最终提交。
## 6. 参考文献
[1] 冈萨雷斯.数字图像处理[M].电子工业出版社.
[2] 周志华.机器学习[M].清华大学出版社.
[3] 李士勇.智能优化算法及其应用[M].哈尔滨工import os
import cv2
import random
import numpy as np
import pandas as pd
import xml.etree.ElementTree as ET
from sklearn.model_selection import train_test_split

# ===================== 配置参数（这里路径指向你现在这个NEU‑DET文件夹） =====================
DATASET_ROOT = "./NEU-DET"
IMG_FOLDER = os.path.join(DATASET_ROOT, "IMAGES")
ANN_FOLDER = os.path.join(DATASET_ROOT, "ANNOTATIONS")

OUTPUT_ROOT = "./neu_processed"
CLASS_NAMES = [
    "crazing",
    "inclusion",
    "patches",
    "pitted_surface",
    "rolled-in_scale",
    "scratches"
]
IMG_SIZE = (200, 200)
RANDOM_SEED = 42
TRAIN_RATIO = 0.7
VAL_RATIO = 0.15
TEST_RATIO = 0.15

random.seed(RANDOM_SEED)
np.random.seed(RANDOM_SEED)

# 创建输出目录
for split in ["train", "val", "test"]:
    for cls in CLASS_NAMES:
        os.makedirs(os.path.join(OUTPUT_ROOT, split, cls), exist_ok=True)


def image_preprocess_vision(img: np.ndarray) -> np.ndarray:
    """OpenCV预处理：灰度、降噪、ROI截取"""
    gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)
    blur = cv2.GaussianBlur(gray, (3, 3), sigmaX=1.2)
    h, w = gray.shape
    roi = gray[int(h*0.1):int(h*0.9), int(w*0.1):int(w*0.9)]
    roi_resize = cv2.resize(roi, IMG_SIZE)
    return roi_resize


def data_augmentation(img: np.ndarray) -> list:
    """数据增强"""
    aug_imgs = [img.copy()]
    aug_imgs.append(cv2.flip(img, 1))
    aug_imgs.append(cv2.flip(img, 0))
    aug_imgs.append(cv2.rotate(img, cv2.ROTATE_90_CLOCKWISE))
    noise = np.random.normal(0, 8, img.shape).astype(np.int16)
    noisy_img = np.clip(img.astype(np.int16)+noise, 0, 255).astype(np.uint8)
    aug_imgs.append(noisy_img)
    return aug_imgs


def parse_xml(xml_path):
    """解析xml，拿到缺陷类别"""
    tree = ET.parse(xml_path)
    root = tree.getroot()
    obj = root.find("object")
    cls_name = obj.find("name").text
    return cls_name


def process_dataset():
    all_samples = []
    img_list = [f for f in os.listdir(IMG_FOLDER) if f.endswith(".jpg")]

    for img_name in img_list:
        img_path = os.path.join(IMG_FOLDER, img_name)
        xml_name = img_name.replace(".jpg", ".xml")
        xml_path = os.path.join(ANN_FOLDER, xml_name)

        if not os.path.exists(xml_path):
            continue
        cls_name = parse_xml(xml_path)
        if cls_name not in CLASS_NAMES:
            continue
        label_idx = CLASS_NAMES.index(cls_name)

        raw_img = cv2.imread(img_path)
        if raw_img is None:
            continue

        roi_img = image_preprocess_vision(raw_img)
        aug_list = data_augmentation(roi_img)

        for idx, aug_img in enumerate(aug_list):
            save_name = f"{cls_name}_{img_name.split('.')[0]}_{idx}.jpg"
            all_samples.append({
                "cls_name": cls_name,
                "label": label_idx,
                "save_name": save_name,
                "image_array": aug_img
            })

    df = pd.DataFrame(all_samples)
    train_df, temp_df = train_test_split(df, train_size=TRAIN_RATIO, random_state=RANDOM_SEED, stratify=df["label"])
    val_df, test_df = train_test_split(temp_df, train_size=VAL_RATIO/(VAL_RATIO+TEST_RATIO),
                                       random_state=RANDOM_SEED, stratify=temp_df["label"])

    def save_split(split_df, split_name):
        for _, row in split_df.iterrows():
            out_path = os.path.join(OUTPUT_ROOT, split_name, row["cls_name"], row["save_name"])
            cv2.imwrite(out_path, row["image_array"])
        csv_path = os.path.join(OUTPUT_ROOT, f"{split_name}_label.csv")
        split_df[["cls_name", "label", "save_name"]].to_csv(csv_path, index=False)

    save_split(train_df, "train")
    save_split(val_df, "val")
    save_split(test_df, "test")

    print(f"数据集处理完成！输出目录：{OUTPUT_ROOT}")
    print(f"训练集:{len(train_df)} 样本，验证集:{len(val_df)} 样本，测试集:{len(test_df)} 样本")


if __name__ == "__main__":
    process_dataset()


import os
import cv2
import pandas as pd
import torch
from torch.utils.data import Dataset
from torchvision import transforms


class NEUDataset(Dataset):
    def __init__(self, root, csv_file, transform=None):
        self.root = root
        self.df = pd.read_csv(csv_file)
        self.transform = transform

    def __len__(self):
        return len(self.df)

    def __getitem__(self, index):
        row = self.df.iloc[index]
        img_path = os.path.join(self.root, row["cls_name"], row["save_name"])
        img = cv2.imread(img_path, cv2.IMREAD_GRAYSCALE)
        label = int(row["label"])
        if self.transform:
            img = self.transform(img)
        return img, label


if __name__ == "__main__":
    trans = transforms.Compose([
        transforms.ToPILImage(),
        transforms.Resize((200, 200)),
        transforms.ToTensor()
    ])
    train_set = NEUDataset(root="./neu_processed/train", csv_file="./neu_processed/train_label.csv", transform=trans)
    print(f"数据集长度 {len(train_set)}")
    img, lab = train_set[0]
    print(f"图片shape {img.shape}, label {lab}")