# 制造智能技术课程设计 - 学习笔记



## 1. Vibe Coding 方法论与 AI 编程工具学习

### 1.1 什么是 Vibe Coding
Vibe Coding 是由 Andrej Karpathy 提出的前沿开发理念，核心理念为 **“我说即所得”** 。开发者通过自然语言描述业务需求与逻辑，由大语言模型（LLM）自动生成、调试并优化代码。在本次课程设计中，我将全程采用该方法，以提升开发效率并聚焦于制造智能场景的业务逻辑闭环。

### 1.2 学习内容与关键策略
根据任务书要求，我重点学习了以下 Vibe Coding 核心技巧：

| 策略 | 具体实践方法 |
| :--- | :--- |
| **提示词工程** | 采用“角色设定 + 任务拆解 + 约束条件 + 验收标准”的结构化模板。避免笼统提问，将复杂系统拆解为前端、API、算法模块等子任务逐一实现。 |
| **上下文工程** | 在让 AI 生成代码前，先提供项目文件树和已有的代码上下文；建立 `project_rules.md` 文件，约束 AI 遵守统一的代码风格和接口规范。 |
| **规格驱动开发** | 先撰写 `需求规格说明书`（SRS），让 AI 基于 SRS 制定分步实施计划，每完成一步进行人工验收，减少返工。 |
| **自动化测试与接管** | 要求 AI 生成单元测试代码；若 AI 反复出错，采取“回滚至上一版本”或“缩小改动范围”的策略，人工接管复杂逻辑。 |

### 1.3 使用的 AI 编程工具
- **主开发工具**：**VS Code**（配合 Cline / Continue 插件，便于无缝接入大模型 API）。
- **辅助工具**：**DeepSeek-V4**（官方 IDE 插件），用于实时代码补全与对话式 Bug 修复。

---

## 2. 拟用 Harness 工具与编程大模型选型

根据任务书推荐的清单（第8节），结合当前工具的易用性、上下文长度及编程能力，我做出如下选型：

### 2.1 编程大模型（核心大脑）
- **首选模型**：**DeepSeek-V4**
  - **选型理由**：拥有 1M Token 的超长上下文窗口，能够完整容纳我的项目代码库（前后端+算法），确保 AI 不遗忘早期的架构设计。其代码生成能力在 HumanEval 评测中表现优异，且目前完全免费，适合学生开发者。
- **备选模型**：**Qwen 3.8**（若 DeepSeek 服务拥挤，作为备用切换）。

### 2.2 Harness / 辅助工具（开发脚手架）
- **首选 Harness**：**Trae**
  - **选型理由**：作为智能 AI 开发环境，Trae 能更好地理解项目文件结构，支持 Agent 模式自主调用工具链，适合快速搭建 B/S 架构 Demo。
- **辅助 Harness**：**通义灵码**
  - **选型理由**：作为阿里云出品，对 Java/Python 的后端框架（Spring Boot/FastAPI）支持较好，用于生成稳定的 CRUD 接口代码。

---

## 3. Git 原理与基本功能学习

版本控制是本次课程设计过程考核（30%）的重中之重。我已完成 Git 安装与 GitHub 仓库配置，并掌握了以下核心原理与功能。

### 3.1 Git 核心原理
- **分布式版本控制**：每个开发者的本地都拥有完整的版本库快照，无需联网即可进行提交、查看历史、分支操作。
- **三棵树结构**：
  1. **工作区 (Working Directory)**：实际修改代码的地方。
  2. **暂存区 (Staging Area / Index)**：临时存放改动的地方，用于筛选需要提交的文件。
  3. **本地仓库 (Local Repository)**：`git commit` 后保存的永久快照。
- **哈希值校验**：Git 通过 SHA-1 算法为每次提交生成唯一的 40 位哈希值，确保代码历史的完整性。

### 3.2 本次开发必备的 Git 功能清单

| 命令 | 功能说明 | 本次用途 |
| :--- | :--- | :--- |
| `git init` | 初始化本地仓库 | 在项目根目录创建 `.git` 隐藏文件夹 |
| `git clone <url>` | 克隆远程仓库 | 将 GitHub 仓库拉取到本地开发环境 |
| `git checkout -b <branch>` | 创建并切换新分支 | 用于开发新功能（如 `feature/backend`） |
| `git add .` | 将所有修改加入暂存区 | 准备提交阶段性成果 |
| `git commit -m "feat: xxx"` | 提交到本地仓库 | **小步提交**，撰写有意义的提交说明（如 `feat`, `fix`, `docs`） |
| `git push origin main` | 推送到远程仓库 | 备份代码并展示开发过程（过程考核依据） |
| `git log --oneline` | 查看提交历史 | 回顾开发步骤，用于答辩展示 |

---

## 4. 选题方向调研

经过对制造场景的检索及公开数据集的筛选，初步确定选题方向如下。

### 4.1 选定方向
- **拟选课题名称**：**基于计算机视觉与机器学习的金属零部件表面缺陷智能检测与分级系统**
- **应用场景**：面向机械加工（如冲压、铸造）产线的质检环节。解决传统人工目检效率低、标准不统一、微小瑕疵易漏检的问题。

### 4.2 覆盖的 3 个核心技术方向（映射表）
根据任务书要求，系统必须实质性地覆盖至少 3 章技术内容，具体规划如下：

| 序号 | 技术方向（对应章节） | 在系统中的实际作用（非装饰性） | 拟采用的技术栈 |
| :--- | :--- | :--- | :--- |
| **1** | **计算机视觉** | **图像预处理与感兴趣区域（ROI）提取**。对原始摄像头采集的金属零件图像进行灰度化、滤波降噪、边缘检测，定位缺陷可能存在的区域。 | OpenCV-Python |
| **2** | **机器学习 / 深度学习** | **核心缺陷分类器**。提取 ROI 区域的特征（纹理、形状），构建 CNN 神经网络模型，对缺陷类型（划痕、凹坑、毛刺）进行分类识别。 | PyTorch / TensorFlow |
| **3** | **优化算法（运筹学）** | **检测阈值自适应优化**。利用历史检测数据，通过遗传算法或粒子群算法动态优化视觉检测的亮度和相似度阈值，以适应不同批次工件的表面差异。 | Scikit-Opt / 自定义遗传算法 |

### 4.3 数据来源
- **主要来源**：**NEU表面缺陷数据库**（东北大学公开数据集，包含热轧带钢的 6 种典型表面缺陷，共 1800 余张灰度图像）。
- **补充来源**：**飞桨 AI Studio** 中的“工业质检公开数据集”。
- *注：若数据量不足，将采用数据增强（翻转、旋转、加噪）扩充样本。*

### 4.4 系统架构初稿（B/S 架构）

```mermaid
graph TD
    A[前端界面 (HTML/CSS/JS)] -->|上传图片/查看报告| B[后端服务 (FastAPI)]
    B -->|存储记录| C[(数据库 SQLite/MySQL)]
    B -->|调用算法| D[算法核心模块]
    D -->|方向1: 预处理| E[OpenCV 视觉模块]
    D -->|方向2: 分类| F[PyTorch CNN 推理模块]
    D -->|方向3: 调优| G[遗传算法优化器]
    E --> F
    G --> E
